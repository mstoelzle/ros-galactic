package:
  name: ros-galactic-moveit-ros-visualization
  version: 2.3.2
source:
  git_url: https://github.com/moveit/moveit2-release.git
  git_rev: release/galactic/moveit_ros_visualization/2.3.2-2
  folder: ros-galactic-moveit-ros-visualization/src/work

build:
  script:
    sel(win): bld_ament_cmake.bat
    sel(unix): build_ament_cmake.sh
  number: 3
about:
  home: https://www.ros.org/
  license: BSD-3-Clause
  summary: |
    Robot Operating System

extra:
  recipe-maintainers:
    - ros-forge

requirements:
  build:
    - "{{ compiler('cxx') }}"
    - "{{ compiler('c') }}"
    - ninja
    - sel(unix): make
    - sel(osx): tapi
    - cmake
    - sel(build_platform != target_platform): python
    - sel(build_platform != target_platform): cross-python_{{ target_platform }}
    - sel(build_platform != target_platform): cython
    - sel(build_platform != target_platform): pkg-config
    - sel(linux): "{{ cdt('mesa-libgl-devel') }}"
    - sel(linux): "{{ cdt('mesa-dri-drivers') }}"
    - sel(linux): "{{ cdt('libselinux') }}"
    - sel(linux): "{{ cdt('libxdamage') }}"
    - sel(linux): "{{ cdt('libxxf86vm') }}"
    - sel(linux): "{{ cdt('libxfixes') }}"
    - sel(linux): "{{ cdt('libxext') }}"
    - sel(linux): "{{ cdt('libxau') }}"
    - sel(linux): "{{ cdt('libxcb') }}"
  host:
    - eigen
    - ogre
    - python
    - qt 5.12
    - ros-galactic-ament-cmake
    - ros-galactic-ament-lint-auto
    - ros-galactic-ament-lint-common
    - ros-galactic-class-loader
    - ros-galactic-geometric-shapes
    - ros-galactic-interactive-markers
    - ros-galactic-moveit-common
    - ros-galactic-moveit-ros-planning-interface
    - ros-galactic-moveit-ros-robot-interaction
    - ros-galactic-moveit-ros-warehouse
    - ros-galactic-object-recognition-msgs
    - ros-galactic-pluginlib
    - ros-galactic-rclcpp
    - ros-galactic-rclpy
    - ros-galactic-ros-environment
    - ros-galactic-ros-workspace
    - ros-galactic-rviz2
    - ros-galactic-tf2-eigen
    - ros2-distro-mutex 0.1 galactic
    - sel(build_platform == target_platform): pkg-config
    - sel(unix): xorg-libx11
    - sel(unix): xorg-libxext
  run:
    - python
    - ros-galactic-geometric-shapes
    - ros-galactic-interactive-markers
    - ros-galactic-moveit-ros-planning-interface
    - ros-galactic-moveit-ros-robot-interaction
    - ros-galactic-moveit-ros-warehouse
    - ros-galactic-object-recognition-msgs
    - ros-galactic-pluginlib
    - ros-galactic-rclcpp
    - ros-galactic-rclpy
    - ros-galactic-ros-workspace
    - ros-galactic-rviz2
    - ros-galactic-tf2-eigen
    - ros2-distro-mutex 0.1 galactic
    - sel(osx and x86_64): __osx >={{ MACOSX_DEPLOYMENT_TARGET|default('10.14') }}
    - sel(unix): xorg-libx11
    - sel(unix): xorg-libxext
